SELECT S.SNUM, S.SNAME, C.CNUM, C.CNAME
FROM SALESPEOPLE S
JOIN CUST C ON S.SNUM = C.SNUM
WHERE C.CNUM IN (
    SELECT CNUM
    FROM ORDERS
    GROUP BY CNUM
    HAVING COUNT(ONUM) > 1
)
ORDER BY S.SNAME, C.CNAME;
