SELECT C1.CNUM AS Customer1, C1.CNAME AS Customer1Name, C2.CNUM AS Customer2, C2.CNAME AS Customer2Name, S.SNAME AS SalespersonName
FROM CUST C1
JOIN CUST C2 ON C1.SNUM = C2.SNUM AND C1.CNUM < C2.CNUM
JOIN SALESPEOPLE S ON C1.SNUM = S.SNUM;
